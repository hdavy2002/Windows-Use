{
  "id": "printer",
  "version": "1.0.0",
  "name": "Printers & Scanners",
  "keywords": ["printer", "print", "scanner", "scan", "print queue", "printing", "paper jam", "default printer", "add printer", "driver"],
  "direct_commands": {
    "open printers": "Start-Process ms-settings:printers",
    "open printer settings": "Start-Process ms-settings:printers",
    "open print queue": "Start-Process ms-settings:printers",
    "add printer": "Start-Process ms-settings:printers"
  },
  "launch": {
    "printers": "Start-Process ms-settings:printers",
    "devices": "Start-Process ms-settings:connecteddevices"
  },
  "diagnostics": [
    "Get-Printer | Select-Object Name,PrinterStatus,PortName | Format-Table",
    "Get-PrintJob -PrinterName * 2>$null | Select-Object -First 10 JobStatus,DocumentName,Size | Format-Table"
  ],
  "prompt": "You are helping with printer/scanner issues. Can open printer settings, check printer status and queues. Common fixes: clear print queue, set default printer, restart Print Spooler service (Get-Service Spooler). For driver issues: remove and re-add printer. For paper jams: guide through physical check and queue clearing."
}
